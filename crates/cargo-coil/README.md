# cargo-coil

`cargo-coil` provides the Cargo subcommand for Coil:

```bash
cargo coil new my-store
```

It owns the interactive project wizard, descriptor-backed regeneration, and basic workspace maintenance commands.
