-- Local Shoppr development uses the built-in `__dev/login/*` routes to issue
-- authenticated customer and admin sessions on first run.
--
-- This seed file is intentionally inert until the customer app defines concrete
-- production seed requirements for its auth package.
SELECT 1;
