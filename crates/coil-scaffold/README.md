# coil-scaffold

Internal scaffold and project lifecycle support for Coil.

This crate powers the `cargo coil` subcommand:

- project initialisation
- project descriptor persistence
- workspace regeneration
- basic workspace maintenance commands
