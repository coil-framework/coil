[package]
name = "davenda-storage"
version = "0.1.0"
edition = "2024"

[dependencies]
davenda-config = { path = "../davenda-config" }
url = "2"
ureq = { version = "2.12", default-features = false, features = ["tls"] }
thiserror = "1.0"
